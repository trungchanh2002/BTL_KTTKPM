describe("Math", () => {
  describe("other test", () => {
    it("should return true if a number is greater than zero", () => {
      // Giả sử bạn muốn kiểm tra nếu một số là lớn hơn 0
      const number = 5;
      expect(number > 0).toBe(true); // Kiểm tra xem số 5 có lớn hơn 0 không
    });
  });
});
